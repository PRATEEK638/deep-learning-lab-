# FRIDAY – core runtime dependencies
# Install with: pip install -r requirements.txt

# Config parsing (Python < 3.11 fallback)
tomli>=2.0.0; python_version < "3.11"

# Excel support
openpyxl>=3.1.0

# Optional: FastAPI service
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# Optional: psutil for system_tool close_app
psutil>=5.9.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
